#include <stdio.h>
/**
 * this program print a quote
 * main: the main function is the only function used
 * return: exit with the value 0
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
